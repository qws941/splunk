# Archived Folders

This directory contains deprecated/legacy folders that are no longer actively used.

**Archived on**: 2025-11-05

## Folders

- ~~`domains/`~~ - **RESTORED** (Required by index.js - NOT deprecated)
- `dashboards/` - Legacy dashboard files (superseded by configs/dashboards/)
- `test-data/` - Old test data (use test-queries/ instead)
- `test-queries/` - Legacy query examples

## Restoration

If you need to restore any of these:
```bash
cp -r archived/folder_name ./
```

## Deletion Schedule

These folders can be safely deleted after 2025-12-31 if not needed.
