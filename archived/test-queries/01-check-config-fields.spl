```spl
# 1. 설정 변경 로그 필드 확인
# 목적: cfgattr, cfgobj, cfgpath 필드가 실제로 있는지 확인
# 실행: Splunk 검색창에 붙여넣기

index=fortianalyzer earliest=-24h type=event subtype=system
    (logid=0100044546 OR logid=0100044547)
| head 20
| table _time, devname, user, logid, cfgpath, cfgobj, cfgattr, action, ui, msg
```

**확인할 것**:
- cfgattr 필드가 있나요? (있으면 ✅, 없으면 ❌)
- cfgattr 값이 100자 넘는 경우가 있나요?
- cfgobj, cfgpath는 정상인가요?

**예상 결과**:
```
_time                devname  user   logid       cfgpath              cfgobj      cfgattr                      action
2025-10-29 10:30:00  FGT-01   admin  0100044546  firewall.policy[10]  policy_10   srcaddr=all dstaddr=all...   edit
2025-10-29 10:25:00  FGT-02   admin  0100044547  system.interface     port1       ip=10.0.1.1 allowaccess=...  set
```
