<?xml version="1.0" encoding="UTF-8"?>
<form version="1.1" theme="dark">
  <label>🔔 Slack Alert Control - FAZ/FMG Alerts</label>
  <description>Slack 알림 ON/OFF 제어 - FortiAnalyzer/FortiManager 알림 관리</description>

  <row>
    <panel>
      <title>Slack Alert Status &amp; Controls</title>
      <html>
        <div id="slack-control-panel">
          <div style="margin: 20px 0; padding: 20px; background: #f5f5f5; border-radius: 8px;">
            <h3 style="margin-top: 0;">📊 Alert Status</h3>
            <div id="status-container" style="margin: 15px 0;">Loading...</div>

            <div style="margin-top: 20px; display: flex; gap: 10px; flex-wrap: wrap;">
              <button onclick="createAllAlerts()" style="padding: 12px 24px; background: #6f42c1; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold;">🚀 Create All Alerts</button>
              <button onclick="toggleAllSlackAlerts('enable')" style="padding: 12px 24px; background: #28a745; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold;">✅ Enable All Alerts</button>
              <button onclick="toggleAllSlackAlerts('disable')" style="padding: 12px 24px; background: #dc3545; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold;">🔴 Disable All Alerts</button>
              <button onclick="refreshStatus()" style="padding: 12px 24px; background: #007bff; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold;">🔄 Refresh Status</button>
              <button onclick="sendTestAlert()" style="padding: 12px 24px; background: #ff9800; color: white; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: bold;">🧪 Send Test Alert</button>
            </div>

            <div id="individual-controls" style="margin-top: 25px;">
              <h4>Individual Alert Controls</h4>
              <div id="rules-container"></div>
            </div>

            <div id="result-message" style="margin-top: 15px; padding: 10px; border-radius: 4px; display: none;"></div>
          </div>
        </div>

        <script><![CDATA[
require(['jquery', 'splunkjs/ready!'], function($, mvc) {
    const ALERT_RULES = [
        'FAZ_Critical_Alerts',
        'FMG_Policy_Install',
        'FMG_Policy_CRUD',
        'FMG_Object_CRUD',
        'Security_High_Severity'
    ];

    const RULE_LABELS = {
        'FAZ_Critical_Alerts': '🔴 FAZ Critical Events',
        'FMG_Policy_Install': '📦 FMG Policy Install',
        'FMG_Policy_CRUD': '✏️ FMG Policy CRUD',
        'FMG_Object_CRUD': '🔧 FMG Object CRUD',
        'Security_High_Severity': '⚠️ Security High Events'
    };

    let currentStatus = {};

    function showMessage(message, type) {
        const resultDiv = $('#result-message');
        resultDiv.removeClass('alert-success alert-danger alert-info')
                 .addClass('alert-' + type)
                 .text(message)
                 .show();
        setTimeout(() => resultDiv.fadeOut(), 5000);
    }

    function refreshStatus() {
        $('#status-container').html('<div style="color: #007bff;">🔄 Loading status...</div>');

        let completed = 0;
        const total = ALERT_RULES.length;

        ALERT_RULES.forEach(function(ruleName) {
            $.ajax({
                url: '/ko-KR/splunkd/__raw/servicesNS/secmon/search/saved/searches/' + ruleName,
                type: 'GET',
                timeout: 5000,
                success: function(response) {
                    const parser = new DOMParser();
                    const xmlDoc = parser.parseFromString(response, 'text/xml');
                    const slackValue = $(xmlDoc).find('s\\:key[name="action.slack"]').text();
                    currentStatus[ruleName] = slackValue === '1';

                    completed++;
                    if (completed === total) {
                        renderStatus();
                    }
                },
                error: function() {
                    currentStatus[ruleName] = false;
                    completed++;
                    if (completed === total) {
                        const foundAlerts = Object.keys(currentStatus).filter(k => currentStatus[k] !== false).length;
                        if (foundAlerts === 0) {
                            $('#status-container').html('<div style="color: #dc3545; font-size: 16px;">⚠️ No alerts found. Please click "🚀 Create All Alerts" button first.</div>');
                            $('#rules-container').html('');
                        } else {
                            renderStatus();
                        }
                    }
                }
            });
        });
    }

    function renderStatus() {
        const enabled = Object.values(currentStatus).filter(v => v).length;
        const total = ALERT_RULES.length;

        let statusHtml = '<div style="font-size: 18px; font-weight: bold; color: ';
        statusHtml += enabled === total ? '#28a745' : enabled === 0 ? '#dc3545' : '#f7bc38';
        statusHtml += ';">' + enabled + ' / ' + total + ' rules enabled</div>';

        $('#status-container').html(statusHtml);

        let rulesHtml = '';
        ALERT_RULES.forEach(function(ruleName) {
            const isEnabled = currentStatus[ruleName];
            const statusBadge = isEnabled ?
                '<span style="color: #28a745; font-weight: bold;">🟢 ON</span>' :
                '<span style="color: #dc3545; font-weight: bold;">🔴 OFF</span>';

            rulesHtml += '<div style="display: flex; align-items: center; justify-content: space-between; padding: 10px; margin: 5px 0; background: white; border-radius: 4px; border: 1px solid #ddd;">';
            rulesHtml += '<span style="flex: 1;">' + RULE_LABELS[ruleName] + '</span>';
            rulesHtml += '<span style="width: 80px; text-align: center;">' + statusBadge + '</span>';
            rulesHtml += '<button onclick="createSingleAlert(\'' + ruleName + '\')" style="padding: 6px 16px; margin-right: 5px; background: #6f42c1; color: white; border: none; border-radius: 4px; cursor: pointer;">➕ Create</button>';
            rulesHtml += '<button onclick="testSingleAlert(\'' + ruleName + '\')" style="padding: 6px 16px; margin-right: 5px; background: #ff9800; color: white; border: none; border-radius: 4px; cursor: pointer;">🧪 Test</button>';
            rulesHtml += '<button onclick="toggleSingleRule(\'' + ruleName + '\', ' + !isEnabled + ')" style="padding: 6px 16px; background: ' + (isEnabled ? '#dc3545' : '#28a745') + '; color: white; border: none; border-radius: 4px; cursor: pointer;">';
            rulesHtml += isEnabled ? 'Disable' : 'Enable';
            rulesHtml += '</button>';
            rulesHtml += '</div>';
        });

        $('#rules-container').html(rulesHtml);
    }

    window.toggleAllSlackAlerts = function(action) {
        const slackValue = action === 'enable' ? '1' : '0';
        const actionText = action === 'enable' ? 'Enabling' : 'Disabling';

        showMessage(actionText + ' all Slack alerts...', 'info');

        let completed = 0;
        const total = ALERT_RULES.length;

        ALERT_RULES.forEach(function(ruleName, index) {
            setTimeout(function() {
                $.ajax({
                    url: '/ko-KR/splunkd/__raw/servicesNS/secmon/search/saved/searches/' + ruleName,
                    type: 'POST',
                    timeout: 10000,
                    data: {
                        'action.slack': slackValue
                    },
                    success: function() {
                        currentStatus[ruleName] = action === 'enable';
                        completed++;
                        if (completed === total) {
                            showMessage('✅ Successfully ' + (action === 'enable' ? 'enabled' : 'disabled') + ' all Slack alerts', 'success');
                            refreshStatus();
                        }
                    },
                    error: function(xhr) {
                        completed++;
                        if (completed === total) {
                            showMessage('⚠️ Some alerts failed to update. Check permissions.', 'danger');
                            refreshStatus();
                        }
                    }
                });
            }, index * 200);
        });
    };

    window.toggleSingleRule = function(ruleName, enable) {
        const slackValue = enable ? '1' : '0';
        const actionText = enable ? 'Enabling' : 'Disabling';

        showMessage(actionText + ' ' + RULE_LABELS[ruleName] + '...', 'info');

        $.ajax({
            url: '/ko-KR/splunkd/__raw/servicesNS/secmon/search/saved/searches/' + ruleName,
            type: 'POST',
            timeout: 10000,
            data: {
                'action.slack': slackValue
            },
            success: function() {
                currentStatus[ruleName] = enable;
                showMessage('✅ Successfully ' + (enable ? 'enabled' : 'disabled') + ' ' + RULE_LABELS[ruleName], 'success');
                renderStatus();
            },
            error: function() {
                showMessage('❌ Failed to update ' + RULE_LABELS[ruleName] + '. Check permissions.', 'danger');
            }
        });
    };

    window.testSingleAlert = function(ruleName) {
        showMessage('🧪 Testing ' + RULE_LABELS[ruleName] + '...', 'info');

        const testMessages = {
            'FAZ_Critical_Alerts': '| makeresults | eval alert_text="🔴 *FAZ Critical Alert (TEST)*\\n출발지: 192.168.1.100\\n목적지: 10.0.0.50\\n심각도: critical\\n메시지: Test alert from dashboard" | table alert_text',
            'FMG_Policy_Install': '| makeresults | eval alert_text="📦 *FMG Policy Install (TEST)*\\n사용자: admin\\n출발지: 192.168.1.10\\n메시지: Test policy install event" | table alert_text',
            'FMG_Policy_CRUD': '| makeresults | eval alert_text="✏️ *FMG Policy CRUD (TEST)*\\n작업: modify\\n사용자: admin\\n객체: firewall_policy\\n메시지: Test policy modification" | table alert_text',
            'FMG_Object_CRUD': '| makeresults | eval alert_text="🔧 *FMG Object CRUD (TEST)*\\n작업: add\\n사용자: admin\\n객체 유형: address\\n메시지: Test object creation" | table alert_text',
            'Security_High_Severity': '| makeresults | eval alert_text="⚠️ *Security High Alert (TEST)*\\n출발지: 203.0.113.100\\n목적지: 10.0.0.20\\n심각도: high\\n메시지: Test high severity security event" | table alert_text'
        };

        const testSearch = testMessages[ruleName] + ' | sendalert slack param.channel="C09DSD6JH2Q"';

        $.ajax({
            url: '/ko-KR/splunkd/__raw/services/search/jobs',
            type: 'POST',
            timeout: 15000,
            data: {
                search: testSearch,
                output_mode: 'json',
                exec_mode: 'oneshot'
            },
            success: function(response) {
                showMessage('✅ Test alert sent for ' + RULE_LABELS[ruleName] + '! Check Slack.', 'success');
            },
            error: function() {
                showMessage('❌ Failed to send test alert for ' + RULE_LABELS[ruleName], 'danger');
            }
        });
    };

    window.sendTestAlert = function() {
        showMessage('🧪 Sending test alert to Slack...', 'info');

        const testSearch = '| makeresults | eval alert_text="🧪 *Test Alert from Splunk Dashboard*\\nTime: " + strftime(now(), "%Y-%m-%d %H:%M:%S") + "\\nMessage: Slack integration is working correctly!" | table alert_text | sendalert slack param.channel="C09DSD6JH2Q"';

        $.ajax({
            url: '/ko-KR/splunkd/__raw/services/search/jobs',
            type: 'POST',
            timeout: 15000,
            data: {
                search: testSearch,
                output_mode: 'json',
                exec_mode: 'oneshot'
            },
            success: function(response) {
                showMessage('✅ Test alert sent successfully! Check your Slack channel.', 'success');
            },
            error: function() {
                showMessage('❌ Failed to send test alert. Check Slack configuration.', 'danger');
            }
        });
    };

    window.createSingleAlert = function(ruleName) {
        if (!confirm(RULE_LABELS[ruleName] + ' 알림을 생성하시겠습니까?\\n기존 Alert가 있으면 덮어씁니다.')) {
            return;
        }

        showMessage('➕ Creating ' + RULE_LABELS[ruleName] + '...', 'info');

        const alertDefinitions = {
            'FAZ_Critical_Alerts': {
                description: 'FortiAnalyzer 크리티컬 이벤트 (Update Fail, Login Fail 제외)',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (severity=critical OR level=critical) | search NOT msg="*Update Fail*" | search NOT msg="*login*fail*" | search NOT msg="*authentication*fail*" | eval src_ip=coalesce(srcip, src, "N/A") | eval dst_ip=coalesce(dstip, dst, "N/A") | eval severity_level=coalesce(severity, level, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="🔴 *FAZ Critical Alert*\\n출발지: ".src_ip."\\n목적지: ".dst_ip."\\n심각도: ".severity_level."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Policy_Install': {
                description: 'FortiManager 정책 설치 이벤트',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (action=install OR msg="*install*policy*") | eval user_name=coalesce(user, "N/A") | eval src_ip=coalesce(srcip, src, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="📦 *FMG Policy Install*\\n사용자: ".user_name."\\n출발지: ".src_ip."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Policy_CRUD': {
                description: 'FortiManager 정책 CRUD 작업',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search object="*policy*" operation IN (add,set,delete,create,modify,remove) | eval operation_type=coalesce(operation, action, "N/A") | eval user_name=coalesce(user, "N/A") | eval object_name=coalesce(object, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="✏️ *FMG Policy CRUD*\\n작업: ".operation_type."\\n사용자: ".user_name."\\n객체: ".object_name."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Object_CRUD': {
                description: 'FortiManager 객체 CRUD 작업',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search object IN (address,service,vip,addrgrp,servgrp) operation IN (add,set,delete,create,modify,remove) | eval operation_type=coalesce(operation, action, "N/A") | eval user_name=coalesce(user, "N/A") | eval object_type=coalesce(object, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="🔧 *FMG Object CRUD*\\n작업: ".operation_type."\\n사용자: ".user_name."\\n객체 유형: ".object_type."\\n메시지: ".message | head 1 | table alert_text'
            },
            'Security_High_Severity': {
                description: '높은 심각도 보안 이벤트',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (severity=high OR level=high) | eval src_ip=coalesce(srcip, src, "N/A") | eval dst_ip=coalesce(dstip, dst, "N/A") | eval severity_level=coalesce(severity, level, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="⚠️ *Security High Alert*\\n출발지: ".src_ip."\\n목적지: ".dst_ip."\\n심각도: ".severity_level."\\n메시지: ".message | head 1 | table alert_text'
            }
        };

        const def = alertDefinitions[ruleName];

        $.ajax({
            url: '/ko-KR/splunkd/__raw/servicesNS/secmon/search/saved/searches',
            type: 'POST',
            timeout: 15000,
            data: {
                name: ruleName,
                description: def.description,
                search: def.search,
                'cron_schedule': '*/5 * * * *',
                'dispatch.earliest_time': '-5m',
                'dispatch.latest_time': 'now',
                'alert.track': '1',
                'counttype': 'number of events',
                'quantity': '0',
                'relation': 'greater than',
                'action.slack': '1',
                'action.slack.param.channel': 'C09DSD6JH2Q',
                'action.slack.param.message': '$result.alert_text$'
            },
            success: function() {
                showMessage('✅ Successfully created ' + RULE_LABELS[ruleName] + '!', 'success');
                setTimeout(refreshStatus, 1000);
            },
            error: function(xhr) {
                if (xhr.status === 409 || xhr.responseText.indexOf('already exists') !== -1) {
                    showMessage('⚠️ ' + RULE_LABELS[ruleName] + ' already exists. Use Web UI to delete first.', 'danger');
                } else {
                    showMessage('❌ Failed to create ' + RULE_LABELS[ruleName] + '. Check permissions.', 'danger');
                }
            }
        });
    };

    window.createAllAlerts = function() {
        if (!confirm('5개의 Slack Alert를 생성하시겠습니까?\\n기존 Alert가 있으면 덮어씁니다.')) {
            return;
        }

        showMessage('🚀 Creating 5 alerts...', 'info');

        const alertDefinitions = {
            'FAZ_Critical_Alerts': {
                description: 'FortiAnalyzer 크리티컬 이벤트 (Update Fail, Login Fail 제외)',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (severity=critical OR level=critical) | search NOT msg="*Update Fail*" | search NOT msg="*login*fail*" | search NOT msg="*authentication*fail*" | eval src_ip=coalesce(srcip, src, "N/A") | eval dst_ip=coalesce(dstip, dst, "N/A") | eval severity_level=coalesce(severity, level, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="🔴 *FAZ Critical Alert*\\n출발지: ".src_ip."\\n목적지: ".dst_ip."\\n심각도: ".severity_level."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Policy_Install': {
                description: 'FortiManager 정책 설치 이벤트',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (action=install OR msg="*install*policy*") | eval user_name=coalesce(user, "N/A") | eval src_ip=coalesce(srcip, src, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="📦 *FMG Policy Install*\\n사용자: ".user_name."\\n출발지: ".src_ip."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Policy_CRUD': {
                description: 'FortiManager 정책 CRUD 작업',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search object="*policy*" operation IN (add,set,delete,create,modify,remove) | eval operation_type=coalesce(operation, action, "N/A") | eval user_name=coalesce(user, "N/A") | eval object_name=coalesce(object, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="✏️ *FMG Policy CRUD*\\n작업: ".operation_type."\\n사용자: ".user_name."\\n객체: ".object_name."\\n메시지: ".message | head 1 | table alert_text'
            },
            'FMG_Object_CRUD': {
                description: 'FortiManager 객체 CRUD 작업',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search object IN (address,service,vip,addrgrp,servgrp) operation IN (add,set,delete,create,modify,remove) | eval operation_type=coalesce(operation, action, "N/A") | eval user_name=coalesce(user, "N/A") | eval object_type=coalesce(object, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="🔧 *FMG Object CRUD*\\n작업: ".operation_type."\\n사용자: ".user_name."\\n객체 유형: ".object_type."\\n메시지: ".message | head 1 | table alert_text'
            },
            'Security_High_Severity': {
                description: '높은 심각도 보안 이벤트',
                search: 'index=fw sourcetype=fw_log earliest=-5m latest=now | search (severity=high OR level=high) | eval src_ip=coalesce(srcip, src, "N/A") | eval dst_ip=coalesce(dstip, dst, "N/A") | eval severity_level=coalesce(severity, level, "N/A") | eval message=coalesce(msg, "N/A") | eval alert_text="⚠️ *Security High Alert*\\n출발지: ".src_ip."\\n목적지: ".dst_ip."\\n심각도: ".severity_level."\\n메시지: ".message | head 1 | table alert_text'
            }
        };

        let completed = 0;
        const total = Object.keys(alertDefinitions).length;
        let successCount = 0;

        Object.keys(alertDefinitions).forEach(function(alertName, index) {
            const def = alertDefinitions[alertName];

            setTimeout(function() {
                $.ajax({
                    url: '/ko-KR/splunkd/__raw/servicesNS/secmon/search/saved/searches',
                    type: 'POST',
                    timeout: 15000,
                    data: {
                        name: alertName,
                        description: def.description,
                        search: def.search,
                        'cron_schedule': '*/5 * * * *',
                        'dispatch.earliest_time': '-5m',
                        'dispatch.latest_time': 'now',
                        'alert.track': '1',
                        'counttype': 'number of events',
                        'quantity': '0',
                        'relation': 'greater than',
                        'action.slack': '1',
                        'action.slack.param.channel': 'C09DSD6JH2Q',
                        'action.slack.param.message': '$result.alert_text$'
                    },
                    success: function() {
                        successCount++;
                        completed++;
                        if (completed === total) {
                            showMessage('✅ Successfully created ' + successCount + '/' + total + ' alerts!', 'success');
                            setTimeout(refreshStatus, 1000);
                        }
                    },
                    error: function(xhr) {
                        completed++;
                        if (completed === total) {
                            showMessage('⚠️ Created ' + successCount + '/' + total + ' alerts. Some may already exist.', successCount > 0 ? 'success' : 'danger');
                            setTimeout(refreshStatus, 1000);
                        }
                    }
                });
            }, index * 500);
        });
    };

    window.refreshStatus = refreshStatus;
    refreshStatus();
});
        ]]></script>

        <style>
.alert-success { background: #d4edda; color: #155724; border: 1px solid #c3e6cb; }
.alert-danger { background: #f8d7da; color: #721c24; border: 1px solid #f5c6cb; }
.alert-info { background: #d1ecf1; color: #0c5460; border: 1px solid #bee5eb; }
        </style>
      </html>
    </panel>
  </row>

  <row>
    <panel>
      <title>📋 사용 방법</title>
      <html>
        <div style="padding: 15px; background: #fff3cd; border-radius: 6px; border-left: 4px solid #ffc107;">
          <h4 style="margin-top: 0;">Dashboard 사용법</h4>
          <ul style="line-height: 1.8;">
            <li><strong>✅ Enable All Alerts:</strong> 5개 알림 전체 켜기</li>
            <li><strong>🔴 Disable All Alerts:</strong> 5개 알림 전체 끄기 (유지보수 시 유용)</li>
            <li><strong>🔄 Refresh Status:</strong> 현재 상태 새로고침</li>
            <li><strong>🧪 Test:</strong> 각 알림별 개별 테스트 (포맷별로 다름)</li>
          </ul>
          <p style="margin-bottom: 0;"><strong>참고:</strong> 변경사항은 즉시 적용됩니다. Alert 자체는 계속 실행되며, Slack 알림만 제어됩니다.</p>
        </div>
      </html>
    </panel>
  </row>
</form>
