━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 실제 FortiGate 7.4.5 로그 확인 (Splunk Web UI에서 실행)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Splunk 접속: https://splunk.jclee.me:8000/app/search/search

2. 아래 쿼리 복사 → 붙여넣기 → 실행

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 쿼리 1: 최근 1시간 로그 10개 (모든 필드 확인)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

index=fw earliest=-1h
| head 10
| table _time, devname, type, subtype, logid, cfgpath, user, action, msg, logdesc, _raw

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 쿼리 2: 설정 변경 로그만 (실제 데이터)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

index=fw earliest=-1h (config OR policy OR address OR service OR cfgpath=*)
| head 20
| table _time, devname, user, action, cfgpath, msg, logdesc

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 쿼리 3: 원본 로그 1개만 (모든 필드명 확인)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

index=fw earliest=-1h
| head 1
| transpose
| rename column AS "필드명", "row 1" AS "값"
| where isnotnull(값) AND 값 != ""

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 쿼리 4: cfgpath 필드 있는 로그만
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

index=fw earliest=-1h cfgpath=*
| head 10
| table _time, devname, cfgpath, user, action, msg

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 쿼리 5: 장비별 로그 수 (최근 1시간)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

index=fw earliest=-1h
| stats count by devname
| sort -count

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📌 확인 사항:

1. 쿼리 1 실행 → 로그가 나오는지 확인
   - 나옴: 어떤 필드들이 있는지 확인
   - 안 나옴: index=fw에 데이터가 없음

2. 쿼리 3 실행 → 실제 필드명 목록 확인
   - cfgpath 필드 있는지?
   - msg, logdesc 필드 있는지?
   - 어떤 필드들이 실제로 존재하는지?

3. 쿼리 4 실행 → cfgpath 필드 데이터 확인
   - cfgpath=* 로그가 있는지?
   - 없으면 FortiGate 7.4.5에서 cfgpath 필드를 안 보내는 것

4. 결과를 이 파일 아래에 붙여넣기:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 실제 데이터 확인 결과 (여기에 붙여넣기)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

[쿼리 1 결과]:


[쿼리 3 결과 - 필드 목록]:


[쿼리 4 결과 - cfgpath 있는 로그]:


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
