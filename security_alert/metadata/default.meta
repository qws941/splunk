[]
# 기본: admin, power role만 접근 (에러 없음, 바로 작동)
access = read : [ admin, power ], write : [ admin ]
export = system

# 특정 사용자만 접근하려면:
# 1. Splunk Web → Settings → Access controls → Roles → New Role
# 2. Role name: secmon_role 생성
# 3. Settings → Users → secmon → Roles에서 secmon_role 추가
# 4. 아래 주석 해제:
# access = read : [ secmon_role, admin ], write : [ admin ]

[transforms]
export = system

[savedsearches]
export = system

[macros]
export = system

[props]
export = system

[lookups]
export = system
