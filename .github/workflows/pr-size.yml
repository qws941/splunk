# PR Size Labeler
# SSoT: qws941/.github — synced to all repos
# Synced via: .github/sync.yml
# Template: _pr-size.yml

name: PR Size

on:
  pull_request_target:
    types: [opened, synchronize]

concurrency:
  group: pr-size-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  pull-requests: write

jobs:
  pr-size:
    uses: qws941/.github/.github/workflows/_pr-size.yml@master
