[]
# 보안팀만 접근 가능하도록 제한
# role 이름을 실제 Splunk role로 변경하세요 (예: security_team, admin 등)
access = read : [ admin, power ], write : [ admin ]
export = system

# 또는 특정 role만 접근:
# access = read : [ security_team, admin ], write : [ admin ]

[transforms]
export = system

[savedsearches]
export = system

[macros]
export = system

[props]
export = system

[lookups]
export = system
